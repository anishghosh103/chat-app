import { SortByLastViewedPipe } from './sort-by-last-viewed.pipe';

describe('SortByLastViewedPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByLastViewedPipe();
    expect(pipe).toBeTruthy();
  });
});
